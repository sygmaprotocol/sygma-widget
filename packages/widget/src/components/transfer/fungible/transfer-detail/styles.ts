import { css } from 'lit';

export const styles = css`
  .transferDetailBox {
    display: flex;
    width: 100%;
    justify-content: space-between;
    margin-bottom: 0.25rem;
  }

  .transferDetailBox-label {
    color: var(--zinc-400);
  }

  .transferDetailBox-value {
    font-weight: 400;
  }
`;
